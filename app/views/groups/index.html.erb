<h1>Каталог</h1>
<!--<%= link_to 'Новый Каталог', new_group_path %>-->


<table>
  <tr>
    <th>Каталог</th>
    <th>Описание</th>
  </tr>
 
  <% @groups.each do |group| %>
    <tr>
      <td><%= group.title %></td>
      <td><%= group.text %></td>
      <td><%= link_to 'Показать', group_path(group) %></td>
      <!--<td><%= link_to 'Edit', edit_group_path(group) %></td>
      <td><%= link_to 'Destroy', group_path(group),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>-->
    </tr>
  <% end %>
</table>

